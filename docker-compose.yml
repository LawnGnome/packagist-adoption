redis:
    image: redis

web:
    build: php
    ports:
        - 8070:80
    links:
        - redis
    volumes:
        - .:/var/www:ro
        - ./php/php.ini:/usr/local/etc/php/php.ini:ro
  
# vim: set et ts=4 sw=4:
